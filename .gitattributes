# ★ CRITICAL ★ this project's preflight gates on the sha256 of source files (final_029.py FROZEN_MODULES).
# Any line-ending conversion changes the bytes → the hashes mismatch → nobody who clones the repo
# can reproduce the frozen verification. So text conversion is disabled repository-wide and every
# file is stored byte for byte.
* -text
