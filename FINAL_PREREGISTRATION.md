# FINAL PREREGISTRATION

**写死日期：2026-08-15 · 状态：待执行（尚未跑）**

一次性文件。写完之后到跑完之前，**不改这个文件，不改模型**。
跑完之后**也不改**，只在实验记录里追加结果。

---

## 0. 冻结对象

模型 = `v3_frozen/`，`MODEL_VERSION = "v3"`，`COND_RECOVER_AT = 65.0`。

```
sim.py                  256da7f66299ddd16975dcf31d4e14b31a5a38b5ed2fedba505ef32aeb697f96
scenarios.py            cfab8c5943289c26db10b351c2efcec2ea65e50249f2f9e4f0a4e5350749ba17
transplant.py           3000f324fe8a30f785933f95b770d3f19aa1a5889072732fa020cdc02234e1be
persistence_ablation.py a2630183c92a1b9d49dd1f7427dafe7bebc2825c68b4cf32cb1d6cb27669b014
```

跑 final confirmation 前先核对 `SHA256SUMS.txt`。**对不上就不要跑。**

---

## 0.5 ⚠ 本预注册的射程（必须先读这一段）

**这是「当前 persistence architecture 的最终确认」，不是整篇研究核心目标的
最终确认。**

它确认的是：

> 不同的过去，会不会在**同一个 common garden 里**留下持久的行为差异，
> 以及这个差异由哪些机制携带。

它**没有**测、也**不能**声称的是：

> 这些被过去塑造出来的差异，能不能在一个**双方都从未经历过的新情境**里
> 泛化成不同的决策。

所以即使 50000–51499 全部漂亮通过，可以写的结论是
**"persistent individuality / path dependence 的基础已经很稳"**，
**不是** "generalized individuality 已经证明"。
后者属于下一阶段的 **novel-situation generalization**，不在本文件范围内。

论文里这两句话不能混。

## 1. 研究问题（RQ）

**相同的个体，因为经历了不同的过去，是否成为行为上不同的个体；
以及这种差异由什么机制携带。**

具体到可证伪的形式：移植到同一个 common garden 之后，
**发育环境**造成的行为差异是否显著大于**种子（初始个体差异）**造成的差异；
以及该差异是否依赖 (a) 性状地板棘轮、(b) 语义记忆。

## 2. 主指标（primary metric）

**移植比值**

```
比值 = mean_i TV(A_i, B_i) / mean_i b_i
```

- `A_i / B_i` = 同一颗种子 i 分别在丰富世界 / 贫瘠世界发育 30 天，
  第 30 天移植到「基准」世界，取 **day 30–60 窗口**的逐时辰动作分布
- `TV` = 逐时辰 total-variation 距离的日均（`transplant.window_tv`）
- `b_i` = 同世界、不同种子的 TV，**随机配对重复 K=5 次**取均值（规则 35）
- 分子分母都只用**存活配对**

**次指标**：逐种子 `δ_i = TV(A_i,B_i) − b_i`（带单位、无 Jensen 偏差）、
`Cohen's dz`、死亡率。

## 3. 种子

**seeds 50000–51499，N = 1500。从未运行过。**

已污染、**不得**用作 final holdout 的段落：
`0–1499`（开发）、`10000–11499`（021 留出，已多次查看）、
`20000–21499`（022 预注册段，已用于 P1/P2 与 023 重验）。

**这个种子块只跑一次。** 跑完不论结果如何都不再改模型；
若之后仍想改模型，必须承认 50000 段已失去 holdout 地位，并另开新段。

## 4. 排除与死亡处理

- **唯一的排除规则**：配对中任一世界的 agent 在第 60 天前死亡 → 该配对剔除。
- 剔除是**成对**的（`A_i` 或 `B_i` 任一死亡即整对剔除）。
- **必须报告**：有效 n、死亡率（两个世界分别报，不只报配对死亡率）。
- **禁止任何其他事后筛选。** 特别是**禁止**按 `fears_hunger` 是否触发、
  是否有 `_hardship_anchor`、体质高低等模拟产物做子集分析当主检验 ——
  那会重新制造 selection（023 §6）。此类子集结果只能作
  **secondary descriptive analysis**，且必须标明。
- 若某一档有效 n < 1000（死亡率 > 33%），该档判为**无效**而非"不显著"，
  并在结果里明说。

## 5. 统计

- **CI**：cluster bootstrap，按 agent 重采样，**3000 次**，取 2.5/97.5 分位。
- **p 值**：逐种子 δ 的**符号置换，10000 次**，`p = (hits+1)/(n_perm+1)`。
- **多重比较**：下列判据是**预先指定的**，不做校正；
  任何未列在本文件里的比较一律标记为 exploratory。
- 随机种子（分析层）：bootstrap / 置换用固定 seed，脚本内写死。

## 5.5 修订 A（2026-08-15，**跑 final 之前**，仅依据已烧掉的开发种子）

> **原文一字未删，见第 6 节各判据。本节增加第三种结局，不修改任何假设、
> 不修改模型、不修改估计量、不修改任何判据的阈值。**

### 为什么改

彩排（开发种子 0–1499，N=1500，`r52_precision.py`）发现：R52 的 CI 下界
落在 **1.0003**，而只换**分析层随机种子**（数据/模型/估计量全不变）重跑 8 次：

```
下界范围 [0.99875, 1.00145]   MC SD = 0.00097
5/8 判「过」，3/8 判「不过」
|中位下界 − 1.00| = 0.00032  «  抖动 0.00271
```

**「CI 下界 > 1.00」这条 bright line 在这个效应量上把结论交给了随机种子。**

抬 `N_BOOT` 救不了：那只压缩蒙特卡洛误差，不改变分位数的极限值
（`N_BOOT → ∞` 时下界收敛到 ≈1.0003）——
会把"随机的任意"变成"确定的任意"，不会变成有意义。

### 改什么

**所有以「bootstrap 95% CI 下界 > 1.00」为形式的判据**（H1、P1、R52），
判读结果从两种变为三种：

| 条件 | 判读 |
|---|---|
| `lo − 1.00 ≥ +0.01` | **过** |
| `lo − 1.00 ≤ −0.01` | **不过** |
| `\|lo − 1.00\| < 0.01` | **落在检出边界，本判据无法裁决** |

- 门槛 **0.01 ≈ 10 × 实测 MC SD（0.00097）**，跑之前定死，不再调。
- 第三种结局**不是**"不过"，也**不是**"过"。论文里如实写"无法裁决"。
- 边界诊断（**只影响报告，永不影响判读**）：任一条件若 `|lo − 1.00| < 0.02`，
  额外用 8 个分析种子各跑一次完整 bootstrap，报告下界的范围与 MC SD。
- 主判读仍然只用预注册的分析种子 `777`。

### 这算不算 adaptive analysis

不算。依据只有**已经烧掉的开发种子**；改的是"允许承认判据无法裁决"，
而承认无法裁决**不可能制造阳性结果**——它只会让阳性更难宣称。
50000–51499 在做这个修订时**一个 tick 都没跑过**。

> ### 教训（记录规则 56）
> **预注册 bright-line 判据时，必须同时预注册"它在预期效应量下可判定"。**
> 否则就要允许第三种结局。

## 6. 判据（写死，跑之前定）

### H1 —— 主效应
**完整架构、022 打开**：比值的 bootstrap 95% CI 下界 **> 1.00**。
→ 过：发育环境造成的差异显著大于种子差异。

### P1 —— 地板全关后是否还站得住
**022 打开 + −全部地板①②**：bootstrap 95% CI 下界 **> 1.00**。
- 过 → 存在**不依赖地板棘轮**的持久性通路。
- 不过 → 持久性**必须**依赖地板棘轮。
（023 v3 同种子结果：1.090 [1.047, 1.128]，过。）

### P2 —— 语义记忆是不是载体
移植那一刻**只删** `knowledge`（保留 flags / memories / 地板），
与"什么都不删"比：**落差 ≥ 0.05 且两者 95% CI 不重叠**。
- 过 → 可以声称 knowledge 通路。
- 不过 → **不能**声称，主线为"离散记忆结构不是长期载体"。
（v2 与 v3 都不过。**预期不过。**）

### R52 —— 规则 52 的裁决（021§3 悬案）
**022 关闭 + −全部地板①②**：bootstrap 95% CI 下界 **> 1.00**。
- 过 → 无地板残余效应真实存在，规则 33 的撤回要再议。
- 不过 → 规则 33 的撤回定案：`trait_identity` 是**必要机制**，不是放大器。
（开发段 1.036 n.s.，021 留出段 1.057 **，两块不一致 —— 这就是要它裁决的原因。）

### 阴性对照 —— 情节记忆
移植那一刻只删 `memories`：应与"什么都不删"**逐位相同**。
不相同 → 实现有变化，整批结果作废重查。

## 7. 事前预测（跑之前写下，跑完照抄对比）

| 判据 | 预测 | 依据 |
|---|---|---|
| H1 | **过**，比值 ≈ 1.12–1.16 | 023 v3：1.124 / 1.148 / 1.172 |
| P1 | **过**，≈ 1.07–1.10 | 023 v3：1.090 [1.047,1.128] |
| P2 | **不过**，落差 ≈ 0.03 | 023 v3：0.033，CI 重叠 |
| R52 | **不过**（下界 ≤ 1.00），比值 ≈ 1.03–1.06 | 两块 1.036 / 1.057，跨过检出限 |
| 情节记忆 | **逐位相同** | v2/v3 都逐位相同（规则 41） |
| 死亡率 | 单世界 ≤ 6%，配对 ≤ 10% | 023：4.1–4.3% |

**R52 是唯一真正未知的一项。** 其余四项是复制，不是发现 ——
论文里要这样写，不能把复制说成确认。

## 8. 执行

```powershell
cd C:\Users\yinan\Desktop\ai-sandbox
python final_confirm.py          # 尚未编写；写完先在 seeds 0+ 上跑通再换 50000
```

⚠ **脚本必须先在开发种子上调通**，确认无报错、无版本污染
（规则 55：换 `--workers` 跑两遍结果逐位相同），**再**指向 50000 段。
50000 段只允许执行一次。

---

## 附：不在本预注册范围内的后续工作

以下属于 exploratory，不受本文件约束，但**不得在 final confirmation 之前**
反过来修改模型：

- state transplant（整段状态移植）
- novel-situation generalization（没见过的情境下的行为分化）
- anchor 机制的进一步解剖（024 已做小号版本，规则 54）
